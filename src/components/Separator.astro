<hr />

<style>
  hr {
    border: 1px solid var(--border-color-active);
    border-bottom: 0;
    border-radius: .5px;
    width: 100%;
    margin: 5vh 0;
    will-change: transform;
    animation-name: separatorColor;
    animation-fill-mode: forwards;
    animation-duration: 5s;
  }

  @keyframes separatorColor {
    from {border-color: var(--border-color-active);}
    to {border-color: var(--border-color);}
}
</style>
