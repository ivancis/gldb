<hr />

<style>
  hr {
    border: 1px solid var(--border-color-none);
    border-bottom: 0;
    border-radius: .5px;
    width: 100%;
    margin: 8vh 0;
    will-change: transform;
    animation-name: separatorColor;
    animation-fill-mode: forwards;
    animation-duration: 5s;
  }

  @keyframes separatorColor {
    from {border-color: var(--border-color-none);}
    to {border-color: var(--border-color);}
}
</style>
